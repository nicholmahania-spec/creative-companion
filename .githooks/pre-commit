#!/bin/sh
# The version bump moved to prepare-commit-msg — it needs the commit
# message to pick patch/minor/major, which doesn't exist yet at this
# point in the commit lifecycle. Nothing to do here currently.
exit 0
