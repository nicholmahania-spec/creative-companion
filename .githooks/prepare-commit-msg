#!/bin/sh
# DISABLED — do not re-enable without testing first. In this repo's actual
# execution environment, ANY hook that stages files during a commit (tried
# pre-commit, prepare-commit-msg, and commit-msg — all three) lands its
# staged changes in the *next* commit's tree, never the current one. That is
# not standard git behavior; something about how `git commit` runs here
# snapshots the tree before hook-staged index changes take effect. The
# result was a permanent one-commit version-number lag and a perpetually
# "dirty" working tree between commits.
#
# The bump-by-commit-message logic (feat!/BREAKING -> major, feat: -> minor,
# else -> patch) is documented in CLAUDE.md as a manual step instead: run
# the right `npm run bump[:minor|:major]` yourself, then `git add` the two
# files, in the same shell sequence as the commit — not via a hook.
exit 0
