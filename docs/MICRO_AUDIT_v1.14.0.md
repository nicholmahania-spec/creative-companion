# Micro audit v1.14.0 — New-user wave

| Check | Result |
|-------|--------|
| DEFAULT_PALETTE / defaultProjectPalette stone | **Pass** |
| No product-default `#4F46E5` in src (tests may use hex as data) | **Pass** (defaults) |
| Onboard does not auto-enable Helper | **Pass** |
| Empty Work primary = Dump an idea | **Pass** |
| Focus capture after onboard | **Pass** |
| Spark pin fallback stone; copy “Board/Work” | **Pass** |
| Settings jump presenceSound | **Pass** |
| Unit tests | **Pass** (39) |
| build:check | **Pass** |

## Residual

- Existing user projects still have indigo if stored  
- Login disabled-primary gray coldness (park)  
