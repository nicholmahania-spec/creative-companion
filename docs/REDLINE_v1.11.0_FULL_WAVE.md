# Redline v1.11.0 — Full residual wave

**Base:** v1.10.0  
**Goal:** Clear the parked queue from UI/UX + humanize cycles.

## Waves

### A — Confirms without `window.confirm`
1. Pack thin → inline panel (Print / Download / Cancel)  
2. Force-break first consent → Settings-style inline or toast path  
3. Delete project / delete step → inline confirm row (keep destructive safety)

### B — Modal focus
1. Extract `useModalFocus(open, rootSelector, { restore })`  
2. Wire lightbox, export, breakdown, onboard  
3. Onboard: focus first field (already autofocus) + Tab trap

### C — Reduce glass + pack grit
1. Header/menu blur → 0–6px or solid stone  
2. Login/onboard pack specimen paper grain border

### D — Helper human reskin
1. Re-export body PNG less CGI / more desk-craft prop  
2. Keep Lottie image path; FAB crop unchanged

### E — i18n + grammar + keys
1. Settings row labels → `ui.*` keys (EN + ar partial OK)  
2. `docs/DESIGN_GRAMMAR.md` stone + 5-step path  
3. Digit keys `1–5` jump journey when not typing  

### F — Soft remaining
- Calendar deadline confirm if easy  
- Demo load confirm → shorter inline if time  

## Acceptance
All A–E items land; tests + build green; push main.  
