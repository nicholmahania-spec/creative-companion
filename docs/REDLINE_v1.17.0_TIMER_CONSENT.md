# Redline v1.17.0 — Timer consent + After strip + Pack polish

## Fixes
1. Force-break enable from Timer/Settings → shared desk consent banner (no silent auto-consent)
2. Timer “After” section: one primary (Mark step done) + text links (not 3 equal cards)
3. Pack page-title display weight for ceremony
4. Settings force-break same consent path

## Acceptance
- Enabling lockouts without prior consent opens alertdialog banner
- Timer After is not a feature-card grid
- Tests + build green
